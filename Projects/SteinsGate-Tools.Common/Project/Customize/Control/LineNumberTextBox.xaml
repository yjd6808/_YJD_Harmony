<UserControl x:Class="SGToolsCommon.Customize.Control.LineNumberTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SGToolsCommon.Customize.Control"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border BorderThickness="0" x:Name="MainBorder">
        <Grid KeyboardNavigation.TabNavigation="None">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="MainTextBox"
                     VerticalScrollBarVisibility="{Binding TextBoxVerticalScrollBarVisibility, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     Grid.Column="1"
                     AcceptsReturn="True"
                     ScrollViewer.ScrollChanged="TextBox_ScrollChanged"
                     FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     TextAlignment="Left"
                     Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     Foreground="{Binding TextBoxForeground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     Background="{Binding TextBoxBackground, RelativeSource={RelativeSource AncestorType=UserControl}}"

                     Padding="0"
                     BorderThickness="0"/>

            <TextBox x:Name="LineNumberedTextBox"
                     Grid.Column="0"
                     FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     IsReadOnly="True"
                     TextAlignment="Right"
                     Text=""
                     Foreground="{Binding LineNumberForeground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     Background="{Binding LineNumberBackground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     Margin="{Binding LineNumberMargin, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     Padding="0"
                     IsHitTestVisible="False"
                     HorizontalAlignment="Left"
                     BorderThickness="0"
                     MinWidth="{Binding LineNumberTextWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </Grid>
    </Border>
    
</UserControl>

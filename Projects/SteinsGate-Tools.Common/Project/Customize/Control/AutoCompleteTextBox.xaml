<UserControl x:Class="SGToolsCommon.Customize.Control.AutoCompleteTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SGToolsCommon.Customize.Control"
             xmlns:ub="clr-namespace:SGToolsCommon.Customize.Behavior"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="ControlMain"
             PreviewKeyDown="AutoCompleteTextBox_OnPreviewKeyDown">
    <Grid>
        <!--
        콤보박스의 디폴트 패딩은 2이다.
        명시적으로 0으로 설정해줘야. TextBox처럼 위화감이 없다.
        -->
        <ComboBox x:Name="AutoCompleteComboBox"
                  IsEditable="True"
                  IsEnabled="{Binding IsEnabled, ElementName=ControlMain}"
                  IsTextSearchEnabled="False"
                  StaysOpenOnEdit="True"
                  VerticalContentAlignment="Center"
                  Padding="0"
                  ItemsSource="{Binding FilteredCandidates, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                  MaxDropDownHeight="190"
                  IsDropDownOpen="False"
                  PreviewMouseDown="AutoCompleteComboBox_OnPreviewMouseDown">
            <ComboBox.Resources>
                <system:Double x:Key="{x:Static SystemParameters.VerticalScrollBarWidthKey}">0</system:Double>
            </ComboBox.Resources>
            <ComboBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel />
                </ItemsPanelTemplate>
            </ComboBox.ItemsPanel>
            <ComboBox.ItemContainerStyle>
                <Style TargetType="{x:Type ComboBoxItem}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                                <Border x:Name="Bd" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                                    <ContentPresenter ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" ContentStringFormat="{TemplateBinding ContentStringFormat}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="TextElement.Foreground" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" TargetName="Bd" Value="#3d3d3d"/>
                                    </Trigger>
                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                        <Setter Property="Background" TargetName="Bd" Value="#3d3d3d"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsSelected}" Value="False">
                                        <Setter Property="Background" TargetName="Bd" Value="#1E1E1E"/>
                                    </DataTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Background" Value="#1E1E1E" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="FontSize" Value="14" />
                </Style>
            </ComboBox.ItemContainerStyle>
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock ub:HighlightTermBehavior.Text="{Binding Text}"
                               ub:HighlightTermBehavior.TermText="{Binding Path=Text, RelativeSource={RelativeSource FindAncestor, AncestorType=local:AutoCompleteTextBox}}"
                               ub:HighlightTermBehavior.Foreground="MediumPurple">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <!--https://stackoverflow.com/questions/20846966/datatrigger-for-textblock
                                스타일 내에서 Foreground, Background 설정해줘야함. TextBlock 자체에다가 설정해놓으면 안됨.
                                -->
                                <Setter Property="Foreground" Value="#DCDCDC"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                        <Setter Property="TextBlock.Background" Value="#3d3d3d" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsSelected}" Value="False">
                                        <Setter Property="TextBlock.Background" Value="Transparent" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Name="PlaceholderTextBlock"
                   Margin="4, 1, 0, 0"
                   Foreground="Gray"
                   VerticalAlignment="{Binding VerticalContentAlignment, ElementName=AutoCompleteComboBox}"
                   HorizontalAlignment="{Binding HorizontalAlignment, ElementName=AutoCompleteComboBox}"
                   IsHitTestVisible="False"
                   Text="{Binding Placeholder, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEmpty, RelativeSource={RelativeSource AncestorType=local:AutoCompleteTextBox}}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsEmpty, RelativeSource={RelativeSource AncestorType=local:AutoCompleteTextBox}}" Value="False">
                            <Setter Property="Visibility" Value="Hidden"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>

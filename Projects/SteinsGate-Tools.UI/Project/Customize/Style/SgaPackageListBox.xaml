<ResourceDictionary x:Class="SGToolsUI.Customize.Style.SgaPackageListBoxStyle"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    mc:Ignorable="d"

                    xmlns:i="clr-namespace:Microsoft.Xaml.Behaviors;assembly=Microsoft.Xaml.Behaviors"
                    xmlns:acb="clr-namespace:AttachedCommandBehavior;assembly=AttachedCommandBehavior"
                    xmlns:sys="clr-namespace:System;assembly=System.Runtime"
                    xmlns:c="http://schemas.xceed.com/wpf/xaml/toolkit"
                    xmlns:local="clr-namespace:SGToolsUI"
                    xmlns:vm="clr-namespace:SGToolsUI.ViewModel"
                    xmlns:ui_res="clr-namespace:SGToolsUI.Resource"
                    xmlns:ui_ctrl="clr-namespace:SGToolsUI.Customize.Control"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    
                    xmlns:cv="clr-namespace:SGToolsCommon.Customize.Converter;assembly=SGToolsCommon"
                    xmlns:sga="clr-namespace:SGToolsCommon.Sga;assembly=SGToolsCommon"
                    xmlns:common_ctrl="clr-namespace:SGToolsCommon.Customize.Control;assembly=SGToolsCommon"
                    xmlns:common_style="clr-namespace:SGToolsCommon.Customize.Style;assembly=SGToolsCommon">

    <Style x:Key="SgaPackageListBoxItemContainerStyle" BasedOn="{StaticResource StretchListBoxItem}" TargetType="ListBoxItem">
        <EventSetter Event="PreviewMouseLeftButtonDown" Handler="OnListBoxItemPreviewMouseLeftDown" />
    </Style>

    <Style x:Key="SgaPackageListBox" TargetType="ListBox">
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate DataType="{x:Type sga:SgaPackage}">

                    <TextBlock Text="{
                        Binding FileNameWithoutExt, 
                        Converter={StaticResource SgaFileNameConverter}, 
                        ConverterParameter={x:Static sga:SgaPackageType.Interface}}">
                        
                        <!-- 리스트박스 아이템 클릭으로 커맨드를 실행하기 위함 
                             더블클릭은 잘되는데 일반 클릭은 삑날때가 있네;;
                         -->
                        <!--<TextBlock.InputBindings>
                            <MouseBinding MouseAction="LeftClick" 
                                          Command="{Binding DataContext.Commander.SelectSgaPackage, RelativeSource={RelativeSource AncestorType={x:Type ListBox}}}" 
                                          CommandParameter="{Binding}" />
                        </TextBlock.InputBindings>-->
                    </TextBlock>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
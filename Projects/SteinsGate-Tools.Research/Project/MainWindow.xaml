<Window x:Class="SGToolsResearch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SGToolsResearch"
        mc:Ignorable="d"
        Title="Research"
        WindowStyle="SingleBorderWindow"
        Width="220"
        Height="300"
        ResizeMode="NoResize"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{x:Static SystemCommands.CloseWindowCommand}"/>
    </Window.InputBindings>
    <Window.CommandBindings>
        <CommandBinding Command="{x:Static SystemCommands.CloseWindowCommand}" Executed="OnCloseWindow"/>
    </Window.CommandBindings>
    <Grid>
        <!-- Research 버튼 메뉴 -->
        <Border Background="#1E1E1E">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel x:Name="menuPanel_" Margin="12,12,12,12">
                    <TextBlock Text="Research Menu"
                               Foreground="#AAAAAA"
                               FontSize="11"
                               FontWeight="Bold"
                               Margin="4,4,4,12"/>
                    <Button Content="01. RTBLogViewer"
                            Click="OnBtnRTBLogViewer_Click"
                            Margin="0,2,0,2"
                            Padding="8,6"
                            HorizontalContentAlignment="Left"/>
                    <Button Content="99. Any"
                            Click="OnBtnAny_Click"
                            Margin="0,2,0,2"
                            Padding="8,6"
                            HorizontalContentAlignment="Left"/>
                    <Separator Margin="0,8,0,8" Background="#444"/>
                    <Button Content="종료"
                            Click="OnBtnExit_Click"
                            Margin="0,2,0,2"
                            Padding="8,6"
                            HorizontalContentAlignment="Center"
                            Foreground="#FF6B6B"
                            FontWeight="Bold"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>

# 프리미티브 셰이프 (Shape3D 신규 + sgfr 튜토리얼 33) — 완료 기록

- 작성일: 2026-08-17
- 계획: `.admin/plans/2026-08-17-프리미티브-셰이프.md`
- 빌드 검증: `Scripts\BuildProject\sgfr.bat -C Debug -P x64` — **성공**

---

## 1. 완료 작업

### sgf (엔진)
| 파일 | 내용 |
|---|---|
| `Sources/sgf/Scene/Shape3D.h` (신규) | Shape3D — 3D 프리미티브 편의 노드. `SetShape(PrimitiveMesh3DType, color)`, Transform 편의 래퍼(SetPosition/SetScale/SetRotationEuler), OnEnter/OnRender 오버라이드 |
| `Sources/sgf/Scene/Shape3D.cpp` (신규) | OnEnter: 프리미티브 메시 조회 + 디폴트 3D 셰이더 키 + baseColor + `Material::Initialize` / OnRender: `RenderSelf()` |
| `Sources/sgf/SgfHeader.h` | Shape3D.h include 추가 |
| `Project/sgf.vcxproj` / `.filters` | Shape3D.cpp/h 등록 (Scene 필터) |

### sgfr (튜토리얼 33)
| 파일 | 내용 |
|---|---|
| `Tutorial/33_PrimitiveShape/33_PrimitiveShape_Main.h/.cpp` (신규) | 씬 1: Shape2D 4종(Rect/Circle/Line/Triangle, DrawCall 1 배칭 검증) / 씬 2: Shape3D 6종(Cube/Sphere/Capsule/Cylinder/Plane/Quad) 자전 애니메이션, SPACE 씬 전환, ESC 종료 |
| `Sources/sgfr/TutorialRegistry.cpp` | 33번 "프리미티브 셰이프 (Shape2D/Shape3D)" 등록 |
| `Project/sgfr.vcxproj` / `.filters` | 33번 파일 + `01. Tutorial\33_프리미티브_셰이프` 필터 등록 |

### 문서
| 파일 | 내용 |
|---|---|
| `Docs/sgfr/33_프리미티브_셰이프.md` (신규) | 튜토리얼 문서 — 개념(bake vs 메시 자동 드로우), 용어 사전, 코드 워크스루, GPU 동작, 실험 |
| `Docs/sgf/04_씬_시스템과_카메라.md` | §3을 "Shape — 2D/3D 편의 노드"로 개편, 3-2 Shape3D 절 추가, §8 요약 갱신 |
| `Docs/sgf/00_README_엔진_개요.md` | 폴더 구조에 Shape3D.* 반영 |

## 2. 빌드 중 수정 사항

1. `Shape3D.cpp` — `pScene_->GetGraphicDevice()` 사용 위해 `sgf/Scene/Scene.h` include 추가 (C2027 수정)
2. `33_PrimitiveShape_Main.cpp` — `jc::TimeSpan`에는 `GetSeconds()`가 없음 → `static_cast<_f32>(_dt.GetTotalSeconds())`로 수정 (29번과 동일 패턴)

## 3. 결과

- sgf 정적 라이브러리 + sgfr (Debug/x64) 빌드 성공
- 콘솔 목차 33번 선택 시: 2D 셰이프 씬(SPACE → 3D) / 3D 셰이프 씬(SPACE → 2D), ESC 종료

## 4. 참고 (추후 확인)

- 튜토리얼 32 문서(`Docs/sgfr/32_프리미티브_메시.md`)는 아직 미작성 상태 — 별도 작업 필요
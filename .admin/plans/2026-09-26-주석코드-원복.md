# 주석처리 코드 원복

## 원칙

- 이번 턴에 삭제한 주석처리 코드(죽은 코드)를 모두 복구한다.
- 살아있는 주석의 문구 정리(rewrites)는 유지한다.
- 세션의 다른 변경(구분자 등)과 겹치지 않는 파일은 HEAD로 원복,
  겹치는 3개(ThreadPool.cpp, Coroutine.cpp, SmartPtr.h)는 surgical 복구.

## HEAD 원복 20파일

Comparator.h, Event.h, Memory.h, Vector.h, ArrayQueue.h,
ListCollection.h, ListNode.h, CollectionStream.h, HashTable_Bucket.h,
MemoryPoolAbstract.h, BinarySearchMemoryPool.h, TypeTraits.h,
DefaultArrayAllocator.h, New.cpp, ArrayQueue.natvis, CommandParser.h,
Socket.h, IOCPOverlapped.h, NormalLock.cpp, RecursiveLock.cpp,
PropertyBase.h (21개, As 블록+매크로 주석 포함)

## 검증

- 원복 후 diff에서 삭제분만 복구됐는지 확인.
- jc/jnet 빌드 (헤더 영향).

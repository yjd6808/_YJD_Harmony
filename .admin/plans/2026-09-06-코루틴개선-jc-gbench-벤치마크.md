# jc_gbench 코루틴 벤치마크 적용 플랜

- 날짜: 2026-09-06
- 소스 문서: `E:\Programming\AIProject\gfdgdr\` (README_CoroutineBench.md, REVIEW_jc_Coroutine.md, Sources, Project)
- 범위: 성능 테스트만 (리뷰 문서의 정확성/안정성 수정은 제외)

## 1. 기존 코드 분석

- `Projects/jc/Sources/jc/Threading/Coroutine.{h,cpp}` 확인 완료
  - `CoRun / CoResume / CoYield / CoVEH / g_cCoMgr / SetPageInitCount 등` 존재
  - `CO_PAGE_SIZE / CO_STACK_SIZE_LOW,MID,HIGH / CO_STACK_PAGE_COUNT_HIGH / cstLow,Mid,High` 존재
  - gfdgdr `CoroutineTest.cpp`가 요구하는 API와 호환됨
- `Projects/jc_gbench` 현재 상태
  - `Sources/jc_gbench/{Container,Pool,PCH,Primitives,Research}` 만 존재, `Threading/` 없음
  - `PCH/Core.h`: String 벤치 ON, IndexMemoryPoolTest ON, CoroutineTest 항목 없음
  - `Project/jc_gbench.vcxproj{,.filters}`: CoroutineTest.cpp 항목 없음

## 2. 작업 목표

- gfdgdr 문서의 적용 가이드대로 jc_gbench에 코루틴 벤치 추가
- Release|x64 빌드 후 벤치 실행 및 결과 정리
- REVIEW 문서의 버그 수정(xchg, DeallocationStack, 안전장치 등)은 이번 작업에서 제외

## 3. 단계

1. `Sources/jc_gbench/Threading/CoroutineTest.cpp` 신규 추가 (gfdgdr 원본 그대로, UTF-8)
2. `Sources/jc_gbench/PCH/Core.h`에 Threading 섹션 + `#define CoroutineTest ON` 추가 (기존 플래그는 유지)
3. `Project/jc_gbench.vcxproj`에 ClCompile 1줄 추가
4. `Project/jc_gbench.vcxproj.filters`에 Threading 필터 + ClCompile 항목 추가
5. `Scripts\BuildProject\jc_gbench.bat -C Release -P x64` 빌드
6. 벤치 실행 (Release x64):
   - `BM_Switch` 반복 5회 aggregates
   - `BM_DeepYield`
   - `BM_Lifecycle`, `BM_StackGrowth`
   - `BM_RoundRobin --benchmark_min_time=2s`
7. 결과 정리 (ns/왕복, ns/생성, 확장 비용 비교)

## 4. 검증

- 빌드 성공 여부
- `jc_gbench.exe --benchmark_list_tests` 에 `BM_Lifecycle/Switch/RoundRobin/DeepYield/StackGrowth` 출력 확인
- 각 벤치 정상 종료 확인

## 5. 주의

- `_alloca`/`CoVEH`/Fiber 관련 MSVC 경고 발생 시 `#pragma warning` 범위에서 조정
- `BM_StackGrowth_Jc_Eager`는 종료 시 `SetPageInitCount(2)` 복원하므로 필터 개별 실행에도 상태 누수 없음
- 파일 수정 4개(Id: cpp, Core.h, vcxproj, filters) — 5개 미만

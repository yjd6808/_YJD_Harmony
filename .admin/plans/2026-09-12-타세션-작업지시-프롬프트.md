# 타 세션 작업 지시 프롬프트 (2026-09-12)

> 아래 블록을 그대로 복사해 다른 세션에 붙여넣어 실행한다.
> 전제 문서 2건(인계·완료기록)은 원격에 푸시되어 있으니 `git pull`로 받는다.

```text
Harmony 프로젝트 TCHAR 전환 후속 작업을 이어서 한다. (전환 본작업은 완료·푸시됨)

0. 먼저 읽을 문서 (순서대로):
   1. .admin/plans/2026-09-12-TCHAR전환-작업현황-인계.md (§0 + §3 + §4-8 + §5 + §6)
   2. .admin/plans/2026-09-12-TCHAR전환-완료기록.md (1~37번)

1. 상태 전제:
   - git clone --recursive https://github.com/yjd6808/_YJD_Harmony.git (서브모듈 필수),
     브랜치 sg-main, git pull (기준 b71189c2)
   - reset/clean 금지, 커밋은 지시 시에만. test_io/, true/는 gtest 잔재물로 무시·커밋 금지
   - x64 고정, 한국어 답변, 파일 UTF-8 저장, 5개 이상 파일 수정 전 사용자 확인,
     주석에 아키텍처 결정 내용 작성 금지
   - 빌드 병렬 실행 금지 (jc.pdb C1041 충돌 전례). 문자셋 전환 시 -Rebuild 권장,
     MB 검증 후 Unicode로 다시 빌드해서 트리 복구

2. 작업 (인계 §4-8 순서대로):
   ① gtest 전체 실행: jc_gtest 415 + jnet_gtest 4, 양쪽 문자셋, §5-5 절차+4-gate.
      exe 없으면 해당 bat로 빌드. MB 실행 후 Unicode 재빌드 복구
   ② %hs 한글 깨짐 판단 후 작업: 대상·선택지(a/b/c)는 §4-8 2번. 양쪽 빌드 검증 포함
   ③ ODBC W 한글 왕복: live DB 없으면 수행 불가. 불가 사유를 완료기록에 남기고 다음
   ④ dead 3건(Q_LOGIN 스텁 등): 별도 승인 없이 손대지 말 것
   ⑤ gbench 링크: Scripts/BuildBenchmark 실행 금지. ClCompile 상태 유지
   ⑥ sgcl: 손대지 말 것 (Q-3 제외 확정)

3. 완료 기준·보고:
   - 항목마다 완료기록 번호 이어서 추가(38번부터) + 인계 §3/§4 갱신
   - 작업 종료 시 트리는 Unicode 빌드 상태로 둘 것
```

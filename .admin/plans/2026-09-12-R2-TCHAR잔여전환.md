# R2 전역 잔여 전환 계획 (2026-09-12)

- 인계 §4-2 세 번째 항목. 06 원본 스크립트가 저장소에 없어 동등 점검식 8종으로 측정했다.
- 결과: `_T()` 미적용 잔재는 ASCII `+=` 2파일뿐이다. 나머지는 narrow 확정(데이터·경계·테스트)이다.

## 1. 측정 결과 (실측, jc + jc_gtest 374파일)

- A `StringUtilT::Format("...")` narrow 포맷: 0건 (수렴)
- B `==`/`!=` narrow: narrow 확정만 (연산자 테이블, `std::string` 테스트, `DateTime::Format` narrow 패턴 API)
- C narrow CRT: 경계 확정만 (`StackTrace` ANSI/DbgHelp + `%hs`済, `Console` MB 분기,
  `ConsoleLogger` UTF-8 바이트 출력, 테스트 진단 출력, 컴파일아웃 `PrintFormat`)
- D `_T()` `%s`: 39건 전수 검토 — `Directory.cpp:191`은 제네릭 `WIN32_FIND_DATA`라 wide 정상,
  나머지 전부 wide 인자 확인 (수정 없음)
- F/G/H/I: `GetBinaryReadErrorMessage` narrow 반환 API 그대로, `+=` ASCII 2파일만 잔류

## 2. 수정 파일 (2개 — 확인 불필요)

1. `Projects/jc/Sources/jc/Wrapper/WinApi.cpp` — `ProtectToString` ASCII `+=` 12곳 `_T()`화
2. `Projects/jc/Sources/jc/IO/PathResolver.cpp` — `Dump()` ASCII `+=` 7곳 `_T()`화

## 3. 검증

1. `Scripts\BuildProject\jc.bat -P x64` → `jc_gtest.bat -P x64` (Unicode)
2. `Scripts\BuildProject-Multibyte\jc.bat -P x64` → `jc_gtest.bat -P x64` (MultiByte)
3. 양쪽 exe 실행: EXIT 0 + `RUN`==`OK`==415 + `FAILED` 0
4. Unicode 재빌드로 트리 복구.
5. 인계 §4 갱신 + 완료기록 번호 추가.

## 4. 결과 (2026-09-12 11:30)

- 수정 2파일 (`WinApi.cpp` 12곳, `PathResolver.cpp` 8곳)
- jc + jc_gtest Unicode/MultiByte 양쪽 빌드 `[OK]`, 전체 실행 양쪽 EXIT 0, **415/415 PASSED**
- Unicode 재빌드로 트리 복구됨. 인계 §4-2 R2 완료(§4-2 전체 완료), 완료기록 25번 추가됨.

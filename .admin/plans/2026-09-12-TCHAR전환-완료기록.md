# TCHAR 전환 완료기록 (2026-09-11 ~ 09-12)

> 인계 문서: `.admin/plans/2026-09-12-TCHAR전환-작업현황-인계.md`
> 완료되는 대로 번호를 이어서 추가한다. 한 항목 = 한 작업.

1. Phase -1 전 프로젝트 Unicode 전환 — 9파일 27곳, `MultiByte` 잔류 0건
2. 빌드 인프라 — `Scripts\Build.ps1 -Multibyte`, `Scripts\BuildProject-Multibyte\` 31종
3. `Type.h` — `_char/_achar/_wchar` + `UNICODE`/`_UNICODE` 동기화 가드
4. `String.h` 별칭 허브 (`AString/WString/String`) + `BasicString.inl` 연결 + 전방선언 7곳 `String.h` 교체
5. `Ascii.h` 템플릿화 + `Digit::HexCharW` + `CRuntime` wide/`_char` 오버로드
6. `StringUtil<CharT>` 클래스 템플릿화 (헤더 + `StringUtil.inl` + `ToNumber` 단일본문 + `ToString` 격하 + `Adopt` + `_countof` + 전역 sed 543곳, sgcl 제외)
7. `StaticString<Size,CharT>` 2파라미터 전환
8. `BasicString` 마무리 + `BasicStringView` 템플릿 전환 + `Comparator`/`Checker` 정리 + `CMessage`/`Stream` String 복귀
9. `jnet/Winsock` Q-5 단일 TCHAR API (`ErrorMessage`/`LastErrorMessage` → `jc::String`, `ErrorMessageMBCS` 삭제)
10. gtest `operator<<` (wide→UTF-8 분리)
11. jc 파일별 마이그레이션 완료 + jc Unicode/MultiByte 양쪽 빌드 `[OK]`
12. `jc_gtest` Tier1 TYPED_TEST 전환 (`StringUtilTest`/`StaticStringTest`/`StringTest`/`StringViewTest`/`ComparatorTest`)
13. `jc_gtest` Tier2 `_T()` bulk + `Logger` fmt `_char*` + `StringConvert.cpp` vcxproj 등록 누락 해소
14. `File::Exist/Size/Delete/Move` 무한재귀 수정 — 전체 실행 스택오버플로우 해소 (`Projects/jc/Sources/jc/IO/File.cpp`)
15. `Stream::ReadString` _char 단위화 — `WriteString`과 대칭 + 리사이즈 후 버퍼 포인터 갱신 (`Projects/jc/Sources/jc/Stream.cpp`)
16. `Path::FileNameLevel(String)` narrow판 동일 정규화 — 슬래시 런 `/` 단일화 + `\\`→`/` (`Projects/jc/Sources/jc/IO/Path.cpp`)
17. `HttpRequest::SetBody` UTF-8 와이어 바이트화 (`Projects/jc/Sources/jc/IO/Http/HttpRequest.cpp`)
18. `StringConvert::ToWide/ToUtf8` 빈 입력 가드 — 빈 입력→빈 반환 (`Projects/jc/Sources/jc/Primitives/StringConvert.cpp`)
19. gtest 잔재물 exe 디렉토리 고정 — `main()` 시작점 `SetCurrentDirectory(exeDir)` + `FileStreamTest` 종료 정리 `c.dat` 삭제 누락 추가
20. 최종 검증 — jc + jc_gtest Unicode/MultiByte 양쪽 빌드 `[OK]`, 전체 실행 양쪽 EXIT 0, **415/415 PASSED**, Unicode 재빌드로 트리 복구
21. `Console.h` TCHAR 전환 (인계 §4-1 완료) — `CSI`/`VT_*` `_T()`화, `VTFore/BackColor/Token` 4종 `const _char*`, `Write`/`WriteLine` 8종 제네릭 CRT(`_tprintf`/`_stprintf_s`/`_puttchar`), `ReadLine`/`ReadKey` 입출력 `_char` + `_UNICODE` 인라인 분기(`wcin`/`cin`, `UnicodeChar`/`AsciiChar`), `ConsoleKeyInfo::KeyChar` `_char` + 호출부 `ToUtf8` 래핑 제거(`ThreadPool.cpp` 3곳, `ConsoleMenuItem.cpp` 9곳) 및 `String::Source()` 직접 전달, `Coroutine` DumpStack·`MemoryPoolAbstract`·`WinApi`·`Extern_Console`(C-ABI narrow 경계 `FromAnsi`/`ToAnsi`)·`ConsoleLogger`(VT 직접 누적)·`ThreadLocal`(`%hs`)·gtest 2종 리터럴 `_T()` — 15파일
22. `jc_gtest` `CoroutineTest.obj` 파일명 충돌 해소 — `jc_gbench/.../CoroutineTest.cpp`와 `jc_gtest/.../CoroutineTest.cpp`가 동명 obj에 덮어씀(MSB8027/LNK4042, gbench 쪽이 이겨 Coroutine 28건 누락·387건으로 집계). 벤치 쪽에 `<ObjectFileName>$(IntDir)CoroutineBenchTest.obj</ObjectFileName>` 부여 후 **415건 복구** (`Projects/jc_gtest/Project/jc_gtest.vcxproj`)
23. `Assert` 부분 전환 D-1 (인계 §4-2 첫 항목, C안 sgcl 제외 전범위) — `__DebugAssertMsgImpl` `_fmt`만 `const _char*` (`__FILE__`/`__FUNCTION__`/`#expect` narrow 유지), 본문 `StringUtilT` + `_tprintf` + narrow 인자 `%hs`, `FileNameLevel` narrow판 유지, 기본 메시지 `_T("메시지 없음")` + 호출부 리터럴 일괄 `_T()`화(스크립트 426+15곳, 매크로 2곳·`.inl` 4곳 수동) + `%hs` 19곳(`typeid`/`_Name()`/`StatementName`/`TransmissionName` `%c` 등) — jc + jc_gtest Unicode/MultiByte 양쪽 빌드 `[OK]`, 전체 실행 양쪽 EXIT 0, **415/415 PASSED**, Unicode 재빌드 복구
24. 파일 I/O 경로 TCHAR화 D-4 — `File::Move(String)`/`Copy(String)`의 narrow 왕복(`ToAnsi`/`ToUtf8`) 제거 후 `_char` 직결(`FileRename`/`FileOpen` + `_T()` 모드). `Copy` 신규 본문에 실패 경로 핸들 정리 포함. narrow 경로 API·`Directory`/`Path` narrow 위임은 경계로 유지 — jc + jc_gtest Unicode/MultiByte 양쪽 빌드 `[OK]`, 전체 실행 양쪽 EXIT 0, **415/415 PASSED**, Unicode 재빌드 복구
25. jc 전역 `_T()` + `%hs` 잔여분 R2 (인계 §4-2 마지막 항목) — 06 원본 부재로 동등 점검 8종 측정. 잔재는 ASCII `+=` 2파일뿐이라 `WinApi::ProtectToString` 12곳·`PathResolver::Dump` 7곳 + 정수 버퍼 `_sntprintf_s` `_T()`화. 나머지는 narrow 확정(연산자 테이블·`std::string` 테스트·`DateTime::Format` narrow 패턴·`StackTrace` ANSI/DbgHelp·`ConsoleLogger` UTF-8 바이트 출력) — jc + jc_gtest Unicode/MultiByte 양쪽 빌드 `[OK]`, 전체 실행 양쪽 EXIT 0, **415/415 PASSED**, Unicode 재빌드 복구
26. Assert 경로 TCHAR화 (사용자 지정, `Path.h` 유지 결정과 함께) — `JC_WIDEN` 2단 매크로 + `JC_FILE`/`JC_FUNC` 선택(`_UNICODE`면 `WIDEN(__FILE__/__FUNCTION__)`, 아니면 narrow. `_FILEW_`는 컴파일러에 없음), `_filePath`/`_functionName` → `const _char*` + `%s` 출력, `Path::FileNameLevel` `_char` buf판 신설(`#ifdef _UNICODE` 가드, MB 중복 정의 회피) — jc + jc_gtest Unicode/MultiByte 양쪽 빌드 `[OK]`, 전체 실행 양쪽 EXIT 0, **415/415 PASSED**, Unicode 재빌드 복구
27. jnet TCHAR 전환 (인계 §4-3) — `_NetLog` 50곳 + `_Log` 19곳 `_T()` 일괄, `WorkerGroup`/`NetLogger` 헤더 `%hs`, R8 `#ifdef _UNICODE`, `IPv4Address`/`IPv4EndPoint` Parse/ctor/`==` → `const _char*` + 본문 `_char`화(`CRuntime::StrToLong`, `StringUtilT::Format`), Q-5 단일 TCHAR API 실제 적용(`ErrorMessage`/`LastErrorMessage` + `ErrorMessageMBCS` 삭제, sgcl 2곳 제외), `jnet_gtest` 4종 테스트 활성화(ByteOrder/IPAddress/IPEndPoint/Socket) + 리터럴 전환 — jnet + jnet_gtest Unicode/MultiByte 양쪽 빌드 `[OK]`, 전체 실행 양쪽 EXIT 0, **jc 415/415 + jnet 4/4 PASSED**, Unicode 재빌드 복구
28. jdb TCHAR 전환 (MySQL Unicode 제외) — MySQL 8파일 `#ifndef _UNICODE` 제외 + 참조 4곳 분기(assert 후 실패 반환), `JDB_HAS_MSODBCSQL` 가드(PCH+TIME2, 없는 환경에서 `default:`로 시끄럽게 실패), `_Log` 66곳 `_T()` + `%hs`, CT 파서 `StringUtilA`화, SQL narrow 바이트↔`String` 경계 명시(`FromAnsi`/`FromUtf8`/`ToAnsi`), SQLServer A-API 호출부 narrow 직결 — jdb Unicode/MultiByte 양쪽 빌드 `[OK]` (단독 테스트 없음). `jdbr`은 양쪽 문자셋 기존 C2131로 제외(리서치 코드, 미변경). Unicode 재빌드 복구(jc/jnet 계열 포함)
29. `StringUtil` 개명 + `Comparator` 자립화 + `StringView` 뒤집기 — `StringUtil<CharT>`→`BasicStringUtil<CharT>`, `StringUtil=BasicStringUtil<_char>` 신설·`StringUtilT` 삭제, 전역 sed 68파일(`StringUtilT::`→`StringUtil::`, `StringUtil<`→`BasicStringUtil<`, sgcl 제외). `Comparator.h` 자립화(`Type.h`+`Functional.h`+전방선언, Vector 직접 포함 제거) + `Comparator.inl` 신설(PCH 순환 해소). `StringView=BasicStringView<_char>` — jc + jc_gtest Unicode/MultiByte 양쪽 빌드 `[OK]`, 전체 실행 양쪽 EXIT 0, **415/415 PASSED**, Unicode 재빌드 복구
30. sg TCHAR 전환 (인계 §4-3 본체, 계획 `.admin/plans/2026-09-12-sg-TCHAR전환.md`) — 실측 576건/39파일(C2665 216건 `_Log`/`Format` 일괄 `_T()`, C2440 112건, C2664 86건, C2672 78건, 연산자·이동 불일치 C2088/C2678/C2679, C2446/C2039 소수). `LogSpecifier.h` `_T()`화(GAME만 `%hs` narrow 유지), `Host::GetName()`→`String`, `InitializeNetLogger`/`DefaultLogger(String)`, `AppConfig` Find `_T()` — sg + sge Unicode/MultiByte 양쪽 빌드 `[OK]`, jc 415/415 + jnet 4/4 재확인, Unicode 재빌드 복구
31. sgs 공통 + sgs_auth/center/lobby/game/query 일괄 — `_Log`/`_T()` sed, main 5종 `Console::ReadKeyWhile` `_T()`, `AuthenticationComponent::SetAccountId` narrow 경계 유지(`StringConvert::FromUtf8` + include) — sgs 본체 빌드 `[OK]`. sgs_auth Unicode 실패는 미완으로 인계 §4-4에서 계속
32. sgs_auth + jdb TCHAR 전환 B안 (인계 §4-4) — 계정 ID 체인 `_char` 일괄(`AuthenticationManager` 6종, `sgs/AuthenticationComponent` 브릿지 제거, `S_AUTHENTICATION_IS`, `Q_LOGIN` 선언 4종 + MySQL부 `#ifndef _UNICODE`/assert·실패 반환. `R_AUTHENTICATION`·`R_LOBBY` 호출부 무수정). `LOG_SPECIFIER_GAME` `%hs` 유지 확정. jdb B안(사용자 승인, 계획 `.admin/plans/2026-09-12-jdb-TCHAR전환.md`): L1 shim·`Script` 3곳 + 컬럼 리터럴 `_T()`, L2 `BindField`→`GetString`·`StatementName`, L3 `IQuery` 필드명 4종 + `SqlServerQuery` 오버라이드(반환·읽기 narrow 유지), L4 `SqlServerStatementBuilder`, L5 ODBC 제네릭 + `ToAnsi` 제거(MySQL 제외 유지). MB 실패 2건 소진(`IDatabase.h:73` C2679, `SQLCHAR` C2440 4곳) — jdb + sgs_auth Unicode/MultiByte 양쪽 빌드 `[OK]`, Unicode 재빌드 복구. 다음은 sgs_center/lobby/game/query 양쪽 빌드 오류 소진
33. sgs_center/lobby/game/query 양쪽 빌드 (인계 §4-5) — center·lobby·query 수정 없이 양쪽 `[OK]`. game만 `main.cpp` 1파일 2곳(`parse_args` C2672: `StringUtilA::TryToNumber` 명시 narrow + narrow 인자 `%hs`) 후 양쪽 `[OK]` — 4종 Unicode 재빌드 복구. 서버 5종 전환 완료
34. 잔무 정리 (인계 §4-6) — `Type.h` 중복 using 3행 제거, `sg/Config.h/.cpp` 삭제(구 `ConfigRuntime`, `AppConfig`와 중복·미등록·무참조), `R_SETUP.cpp` narrow `%s`→`%hs` 4곳 — sg + sgs_center Unicode/MultiByte 양쪽 빌드 `[OK]`, Unicode 재빌드 복구
35. 전체 13종 최종 검증 sweep (인계 §4-7) — Unicode→MultiByte→Unicode 3-pass 39/39 `[OK]`, 수정 0건. gtest 전체 실행 제외(14:00 이후 jc/jnet 동작 변경 없음) — Unicode 재빌드 복구
36. 범위밖 TCHAR 전환 (인계 §4-9, Q-3 뒤집기 승인) — sgf 24파일 + sgfr 45파일(한글 테이블 `_char`, Main stray `}` 제거) + jnetr 12파일(무수정 통과) + mcp/mcpr + gbench 23파일(`StringUtilA`, `BenchR0=AString`, ClCompile만) + jdbr 양쪽 통과(C2131 해소). jcr 18건 파손 폐기. 부수 `Console.h:244` `Math::Min<int>` — 영역별 U→MB→U 재빌드 복구. sgcl 제외 유지
37. 인계 확정 (2026-09-12 21:30, 타 컴퓨터 이어받기용) — 푸시 `1c96336b` + `acb03579`, 트리 정리(junk 제외), gtest 미실행·%hs 판단·ODBC 왕복·dead 후보·gbench 링크는 인계 §4-8로 이관. 다음 번호 38번
38. gtest 전체 실행 (인계 §4-8 1번, 2026-09-12 22:25) — `jc_gtest` 415 + `jnet_gtest` 4, Unicode/MultiByte 양쪽 §5-5 절차+4-gate 전부 충족 (① EXIT 0 ② RUN==OK ③ FAILED 0 ④ PASSED 수 일치). exe가 없어(`clean -fdx`로 `Output/` 삭제됨) 해당 bat로 빌드. NuGet `packages/`도 함께 삭제되어 있었으므로 동일 버전(1.8.1.5)을 로컬 타 프로젝트에서 복사 복원(gitignore라 커밋 영향 없음). MB 첫 실행은 외부 종료로 197번째에서 중단되어 재실행 후 415/415 통과. MB 실행 후 Unicode 재빌드 복구됨
39. `%hs` 한글 판단+작업 (인계 §4-8 2번, c안, 2026-09-12 22:25) — 대상 `LOG_SPECIFIER_GAME` + `R_SETUP.cpp` 2행 4곳 + narrow 원본(`SteinsGate_Server.h` `ServerType`/`GameServerType`/`ServerProcessType`, `ServerEnum.h` `CenterOrder`/`ServerBootState`, narrow 리터럴은 `/utf-8`로 UTF-8 확정·vcxproj 전수 확인). (a) 유지는 Unicode 로그 깨짐 잔류라 기각, (b) `_char` 테이블화는 sgcl 5곳(`R_AUTHENTICATION`·`UI_Login`·`C_LOBBY`·`C_AUTH`)이 narrow 그대로 써서 Q-3 재논의 필요라 보류, (c) 사용처 `FromUtf8` 채택(`FromUtf8`은 Unicode에서 UTF-8→wide·MB에서 identity라 양쪽 안전, `DescMgr_Item.cpp` 동일 선례). 수정 3파일(`LogSpecifier.h` `%hs`→`%s`, `R_SETUP.cpp` 4인자 래핑+include, `sgs_game/main.cpp` 1인자 래핑+include) — jdb + sgs_center + sgs_game Unicode/MultiByte 양쪽 빌드 `[OK]`, Unicode 재빌드 복구됨
40. ODBC W 한글 왕복 실검증 수행 불가 (인계 §4-8 3번, 2026-09-12 22:25) — 사유 4종: ① SQL Server용 ODBC DSN 없음(조회 결과 파일 DSN만 존재) ② 접속 credential·한글 검증용 테이블/스키마 없음(1433 개방 확인되나 왕복 검증 불가) ③ 인계 전제상 `sgs_query` 실쿼리 작성 시점이 트리거라 미도래 ④ 코드는 빌드만 통과 상태 유지. live DB 준비 후 재시도
41. dead·gbench·sgcl 유지 확인 (인계 §4-8 4·5·6번, 2026-09-12 22:25) — dead 3건(`Q_LOGIN` 스텁·`colNameMap_` 공집합·`AuthenticationManager.cpp:304`) 무수정, `Scripts/BuildBenchmark` 미실행·gbench 무수정(ClCompile 상태 유지), sgcl 무수정(Q-3 제외 유지). `git status` 수정분은 39번 3파일만 + `test_io/`·`true/` 잔재(무시·커밋 금지)
42. jcr 빌드 실패 확인 (2026-09-12 22:33) — 파손은 전 세션에서 이미 복원됨(수정분 0·`_T("$3")` 잔재 0). Unicode 실패(`04_CoStack1.cpp` C2665 미전환 narrow 리터럴 `_LogError_("...")`), MB 실패(`06_Coroutine_CoBasic.cpp` 삭제된 `StringUtilT::FormatBuffer` 잔재). 미전환 리서치 코드라 전환 작업(18파일 규모)이 필요하며 승인 전 손대지 않음
43. gbench 링크 해소 (인계 §4-8 5번, 2026-09-12 22:40) — `BuildVS2022.bat`는 Win32 구성에서 중단(x86 C 컴파일러 검출 실패·CMakeError.log empty, 환경 문제. x64 구성은 C/C++ 정상). x64만 수동 구성+Debug 빌드 → `ThirdParty/Build/x64/lib/Debug/benchmark.lib` 생성(gitignore라 커밋 영향 없음) → `jc_gbench` + `jnet_gbench` Unicode/MultiByte 양쪽 링크 `[OK]`. Unicode 재빌드 복구됨(jc·jnet·jdb·center·game·gbench 2종)
44. gbench 빌드 스크립트 정리 (2026-09-12 22:55) — `BuildVS2022.bat` Win32 블록 삭제(x64 전용, cp949 바이트 보존 편집). `jc/jnet_gbench.bat` Unicode/MultiByte 4종에 lib 사전검사 추가(`ThirdParty\Build\%PLATFORM%\lib\%CONFIG%\benchmark.lib` 기준, vcxproj PreBuildEvent와 동일 경로. 문자셋 무관 단일 lib). 없으면 `BuildVS2022.bat` 안내 후 즉시 종료. `-C Release`로 차단 동작 검증됨(Release lib은 미생성 상태라 `BuildVS2022.bat` 실행 필요)

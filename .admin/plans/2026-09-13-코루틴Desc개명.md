/*
 * 작성자 : 윤정도
 * 코루틴 Desc/Spec/Slab 명명 정리 plan
 */

# 2026-09-13 코루틴 Desc/Spec/Slab 명명 정리

## 배경
- 1건(threadId_/schedRegs_/callerCtx_/pad)은 반영 완료 확인됨 (jc 3종 일치).
  - `threadId_` 읽기처 전무, `CoMgr` 자체가 `thread_local`이라 삭제 정당.
  - 필요시 `Thread::GetThreadId()` 대체 가능.
- 2건 잔여: `CoStackSpec`/`CoDesc` 멤버에 trailing `_` 미적용.

## 범위 (확정, 사용자 확인됨)
- `Projects/jc/Sources/jc/Threading/Coroutine.h`
  - `CoStackSpec { tier_, size_ }`, `CoDesc { spec_, userData_, ppOut_, onException_ }`
  - 팩토리 `Low/High/Custom/FromTier` 내부 `s.tier`/`s.size` 수정
  - `CoRunFn` 내부 `desc.userData` 수정
- `Projects/jc/Sources/jc/Threading/Coroutine.cpp`
  - `CoRun` 내 `_desc.spec.tier` 등 5행 수정
  - `StackSlab { pBase_, slotBytes_, slotCount_, used_ }` + `AllocStack`/`Clear` 사용처 수정
- 호출처 designated initializer 일괄 수정
  - `jc_gtest/Threading/CoroutineTest.cpp` (~18건: `.spec`→`.spec_`, `.ppOut`→`.ppOut_`, `.userData`→`.userData_`, `.onException`→`.onException_`)
  - `jc_gbench/Threading/CoroutineTest.cpp` (6건)
  - `jcr/06_Coroutine_06_Custom.cpp`, `06_Coroutine_14_Api.cpp`, `06_Coroutine_CoBasic.cpp`, `06_Coroutine_05_Safety.cpp`
- 제외: `CoVehStats(maxDispatchUsed/minRemain)`는 이번 범위 밖. 별도 보고만.

## 검증
- `Scripts\BuildProject\jc.bat -C Debug -P x64` (Unicode)
- 필요시 `jc_gtest` 빌드 후 코루틴 테스트 통과 확인
- `static_assert` 오프셋 불변 확인 (이번 변경은 오프셋 영향 없음)

# sg TCHAR 전환 계획 (2026-09-12)

- 인계 §4-3 마지막 본체 단계 (sgcl 제외). sg Unicode 빌드는 기준 broken 상태.
- 전제 수정(금일): `Comparator.h` 자립화 + `Comparator.inl` 신설로 PCH 순환 해소.
  Q-2 C안 유지 (표시용 테이블 TCHAR, 변환은 엔진 교체 시점).

## 1. 측정 (실측, 첫 빌드 오류 576건/39파일)

- C2665 216건: `_Log`/`Format` narrow 리터럴 → `_T()` 일괄 (jnet 방식 sed)
- C2440 112건: narrow → `String` 대입/반환 → `_T()` + 경계 명시
- C2664 86건: API 인자 불일치 (WinAPI-A 직접 호출, narrow 콜백 등) — 건별 판단
- C2672 78건: `Console` narrow → `_T()`
- C2088 30건/C2678 36건/C2679 6건: 연산자·이항 불일치 — 건별 판단
- C2446/C2039 소수: 멤버·반환 정리
- 집중 파일: `CLIFunctions` 114, `Logo` 70, `DescMgr_Item` 52, `DescLoaderAbstract` 50,
  `AppConfig` 46, `CharBase` 28, `DescLoaderMgr` 28, `JsonUtil`·`TextUtil`·`ClientText` 등

## 2. 제외 (별도)

- `sg/Config` 삭제·`Type.h` 중복 정리 등 TCHAR 무관 정리 (인계 §4-3 분리 항목)
- `sgs`/`sgs_*` 6개 — sg 완료 후 별도 단계

## 3. 검증

1. `Scripts\BuildProject\sg.bat -P x64` (Unicode)
2. `Scripts\BuildProject-Multibyte\sg.bat -P x64` (MultiByte)
3. sg 단독 테스트 없음 — 양쪽 빌드 통과로 판정 (+ 의존 `sge` 빌드로 간접 확인)
4. Unicode 재빌드로 트리 복구 (jc/jnet/jdb 계열 포함).
5. 인계 §4 갱신 + 완료기록 번호 추가.

# 완료: sgf jc타입 마이그레이션

- 날짜: 2026-08-15
- 계획: `.admin/plans/2026-08-15-sgf-jc타입-마이그레이션.md`
- 빌드: `Scripts\BuildProject\sgf.bat -C Debug -P x64` — 성공 (오류 0)

## 수행 내역

| 항목 | 처리 |
|---|---|
| `int` → `_s32` (160건, 전 파일) | 위치 보존 마스킹(주석/문자열/raw string 보호) 일괄 치환 — 잔여 0 |
| `float` → `_f32` | GraphicDevice.cpp viewport, RenderTarget.cpp clearColor — 잔여 0 |
| `double` → `_f64` | FrameTimer.cpp deltaMicro 변환 — 잔여 0 |
| `char` 버퍼 → `_s8` | ResourceMgr.cpp:275 `char buffer[512]`, ShaderProgram.cpp:70 `char* pBuffer`/`char[size+1]` |
| `vIndex` → `addressVIndex`, `uIndex` → `addressUIndex` | RenderStates.cpp GetSamplerState (대칭 네이밍) |
| `vkCode` → `virtualKeyCode` | InputManager.cpp:49,65 |
| `FILE* f` → `FILE* pFile` | Renderer2D.cpp:328 (DEBUG-DIAG 블록) |
| 루프 `v` → `corner` | Renderer2D.cpp DrawCircle 정점 루프 |
| 인코딩 | IResource.h, PipelineState.cpp/h → UTF8 BOM + CRLF (sgf 전 파일 BOM+CRLF 통일) |

## 검증 결과 (재스캔)

- 금지 접두사 dw/ui/i/v — 0건
- int/float/double/char — 버퍼용 0건 (`const char*` 셰이더 소스는 허용)
- printf, m_/k*_v, 논-Allman, 한줄 제어문, constexpr camelCase, 파라미터 `_`, 멤버 suffix `_` — 0건
- BOM/CRLF — 전 파일 UTF8 BOM + CRLF

## 유지 대상 (규칙상 허용)

- `const char*` — HLSL 셰이더 소스/문자열 리터럴 (sgfr과 동일)
- `UINT`/`D3D11_*`/`WPARAM` 등 Windows·D3D 타입
- HLSL raw string 내부 float/int — 그대로

## 참고 (후속 확인 권장)

- `Renderer2D.cpp:338-339` — `DrawIndexed(indexCount, 0, 0)` 호출이 2회 중복되어 있음 (디버깅 흔적으로 추정)
- `Renderer2D.cpp:326-336` — `[DEBUG-DIAG]` 임시 로그 블록 (`sgfr29_diag.log` fopen_s/fprintf) — 29번 튜토리얼 디버깅 용도, 제거 여부 미확정

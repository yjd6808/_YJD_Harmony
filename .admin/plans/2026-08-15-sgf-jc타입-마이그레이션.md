# 계획: sgf jc타입 마이그레이션

- 날짜: 2026-08-15
- 대상: UltimateHarmony 메인 워크트리
- 범위: `Projects\sgf\Sources\sgf` (그래픽스/코어/입력/씬/오디오) — 사용자 확인 완료(전체 진행)
- 기준: `.opencode/rules/coding-conventions_cpp.md` + sgfr 적용 선례 (sgfr은 이미 동일 규칙 적용 완료)

## 분석 요약 (스캔 결과)

| # | 위반 | 건수 | 대상 |
|---|---|---|---|
| V1 | `int` → `_s32` | 158 | 전 파일 (루프, 파라미터, 멤버, static_cast, 배열 크기) |
| V2 | `float` → `_f32` | 3 | GraphicDevice.cpp:428-429 (viewport), RenderTarget.cpp:186 (clearColor) |
| V3 | `double` → `_f64` | 2 | FrameTimer.cpp:92-93 |
| V4 | `char` 버퍼 → `_s8` | 3 | ResourceMgr.cpp:275 `char buffer[512]`, ShaderProgram.cpp:70 `char* pBuffer`/`char[size+1]` |
| V5 | 금지 접두사 `vIndex` | 7 | RenderStates.cpp:226-260 (`uIndex`는 허용 접두이나 대칭상 `addressUIndex`로 함께 변경) |
| V6 | `vkCode` (v 접두) | 2 | InputManager.cpp:49,65 → `virtualKeyCode` |
| V7 | 포인터 로컬 p접두 누락 | 1 | Renderer2D.cpp:328 `FILE* f` → `pFile` |
| V8 | BOM/CRLF | 3 | IResource.h, PipelineState.cpp/h (LF+무BOM → UTF8 BOM+CRLF) |

## 예외 (치환 금지)

- HLSL raw string `R"(...)"` 내부 — float/int 정상이므로 유지 (위치 보존 방식으로 자동 보호)
- `const char*` (셰이더 소스/문자열 리터럴) — sgfr과 동일하게 허용
- `UINT`/`DWORD`/`WPARAM`/`LRESULT` 등 Windows/D3D 타입 — 유지
- 문자열 리터럴·주석 내부 — 유지
- `_f32`/`_s32` 등 이미 적용된 jc 타입 — 중복 치환 방지 (워드 경계 매칭)

## 조치 단계

1. **K1: 타입 치환 스크립트** — 위치 보존 방식(주석/문자열/raw string을 같은 길이 공백으로 마스킹 후 원본 인덱스 치환)으로 `int`→`_s32`, `float`→`_f32`, `double`→`_f64` 일괄 적용
2. **K2: 네이밍 수정** — `vIndex`→`addressVIndex`, `uIndex`→`addressUIndex`, `vkCode`→`virtualKeyCode`, `FILE* f`→`FILE* pFile`, Renderer2D.cpp:284 루프 변수 `v`→`corner`
3. **K3: char 버퍼 → _s8** — ResourceMgr.cpp:275, ShaderProgram.cpp:70
4. **K4: 인코딩 변환** — IResource.h, PipelineState.cpp/h 를 UTF8 BOM + CRLF로 재저장
5. **K5: 빌드 검증** — `Scripts\BuildProject\sgf.bat -C Debug -P x64` (성공 기준: 오류 0)
6. **K6: 재스캔** — 위반 0건 확인
7. **K7: 완료 기록** — `.admin/histories/2026-08-15-sgf-jc타입-마이그레이션.md`

## 확인 필요

- (완료) 사용자: 전체 진행 확정 (질문 툴 응답)
- Renderer2D.cpp:326-336 `[DEBUG-DIAG]` 임시 로그 블록(`FILE* f`) — 제거 여부는 사용자 확인 후 결정, 일단 pFile로 rename만

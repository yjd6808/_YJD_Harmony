# 불필요 설명 롤백

## 원칙 (사용자 지시)

- 설명은 필수 아님. 함수명만으로 유추 가능하면 설명 생략.
- 이번 세션에서 새로 지은 설명만 제거한다.
- 기존에 있던 설명(형식만 바뀐 것)은 유지한다.
- 구분자 자체는 유지한다. 설명 제거 후 단일 구분자 1줄로 둔다.

## 제거 대상 (에이전트 보고 + Fill 6종 기준 exact match)

- Fill.cpp 팩토리 6종 신규 설명.
- jc: Config 2, Memory 5, Digit 1, Stream 1, ConsoleMenuItem 2,
  SpinLock 4, WaitHandle 1, ThreadPool 1(84), PooledMemoryChunk 1,
  WinApi 18, CMessage 24, Directory 1, PropertyStatics 4.
- sgf: Scene2D DeclareStatic 6, Window Utf8ToWide 1.
- sgfr: Practice 05-08 GetShaderSource/_Main 8.

## 유지 대상

- 기존 설명의 형식 변환 (`//` → `// :` + 하단 구분자).
- ThreadPool 11/72, RenderStates 고아 블록, Material/GameObject 이중 정리.
- Path.cpp 하단 보강, DrawSprite/QueryCounter 형식 변환.

## 검증

- 제거 스크립트는 파일+문구 exact match만 처리.
- git diff 전수 확인 후 3종 빌드.

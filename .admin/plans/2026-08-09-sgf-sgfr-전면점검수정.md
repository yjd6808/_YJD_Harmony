# 계획: sgf/sgfr 코드 전면 분석 및 수정

- 날짜: 2026-08-09
- 대상: UltimateHarmony 메인 워크트리 (`sg-main`)
- 범위: `Projects\sgf`, `Projects\sgfr` 내 코드만 수정 (jc 등 타 프로젝트 무수정)
- 플랜 근거: 3개 병렬 탐색 에이전트 분석 + 핵심 의심 지점 직접 검증

## 분석 요약

### 1. 확정 버그 (직접 검증 완료)

| # | 위치 | 문제 |
|---|---|---|
| B1 | `sgf\Scene\Camera.cpp:145` | `WheelDelta()`가 이미 틱(120 나눈 값)인데 `/120.0f` 재나눗셈 → 2D 줌 동작 안 함 |
| B2 | `sgf\Scene\Camera.cpp:323` | 동일 → 3D 돌리 동작 안 함 |
| B3 | `sgfr\08_Camera_Main.cpp:129` | `wheel / 120` 정수 나눗셈(0) → 줌 동작 안 함 |
| B4 | `sgfr\13_Input_Main.cpp:58` | `_delta / 120` 정수 나눗셈(0) → 휠 크기 조절 안 됨 |
| B5 | `sgf\Input\InputManager.cpp:107` | `m_WheelDelta =` 덮어쓰기 → 같은 프레임 다중 휠 이벤트 누락. `+=` 누적 필요 |
| B6 | `sgf\Core\Application.cpp:48` | 전역 포인터를 시작에 세팅하지만 실패 리턴에서 nullptr 안 함 → dangling |
| B7 | `sgf\Scene\Director.cpp:157-190` | Update 루프가 `SceneSlot&` 참조 유지 중 OnUpdate 호출 → 씬 교체(벡터 재할당) 시 참조 무효화 UB |
| B8 | `sgf\Scene\Camera.cpp:53-82` | 2D API(SetPosition2D/ZoomBy 등)에 3D 모드 가드 없음 → 3D 상태 오염 |
| B9 | `sgf\Scene\Camera.cpp:341-347` | Dolly 클램프가 dist<0.1이면 음수 → 카메라가 뒤로 밀림 |
| B10 | `sgf\Core\Window.cpp:119-129` | `Destroy()`가 `m_bClosed` 미갱신 → 상태 불일치 |
| B11 | `sgf\Input\InputManager.cpp:146-161` | MouseButton 인덱스 범위 검사 없음 (배열 밖 접근 UB) |
| B12 | `sgf\Graphics\SwapChain.cpp:31,38` | Attach/GetBuffer 실패 경로 누수, 재초기화 시 구체 누수 |
| B13 | `sgf\Graphics\Renderer2D.cpp:131-136` | 흰 텍스처 생성 실패 시 `new Texture()` 누수 |
| B14 | `sgf\Graphics\Shader.cpp:86,97,106` | 재컴파일(핫리로드) 시 기존 셰이더/레이아웃 누수 |
| B15 | `sgf\Graphics\Texture.cpp:190` | 재초기화 시 기존 SRV 누수 |
| B16 | `sgf\Graphics\DepthStencilSurface.cpp:30-37` | 재초기화 시 기존 DSV 누수 |
| B17 | `sgf\Graphics\Buffers.h:122-133` | `UpdateAndBind`가 Map 실패 검사 안 함 + VS/PS 이중 바인딩 |
| B18 | `sgf\Audio\SoundEngine.cpp` | CoInitialize/CoUninitialize 불균형, WAV 2GB+ 음수 오버플로(243행쯤), 10ms 미만 톤 페이드 겹침 |

### 2. 구조 문제 (기약 후보)

| # | 위치 | 문제 |
|---|---|---|
| S1 | `Renderer2D/3D` | Begin/End/Flush/VB/CB 중복 60% → 공용 배치 베이스 추출 (큰 작업) |
| S2 | `GraphicDevice.cpp` | BeginFrame 표면 바인딩 4중 중복 → private 헬퍼 |
| S3 | `Renderer3D.cpp` | FlushTriangles/FlushLines 중복 → 공용 헬퍼 |
| S4 | `SwapChain/Depth/Texture` | `_` 접미사 vs `m_` 접두사 네이밍 혼재 |
| S5 | `SgfMath.h` | 낮은 가드(0 나눗셈) 없음, 미사용 API(Lerp/Rad2Deg/Transposed/SRT2D/RotationX/Y/TransformPoint) 데드 코드 |
| S6 | `sgfr` | 네임스페이스 누락 패턴 조사, 씬/리소스 로딩 실패 무시(숫자 여러 개) |
| S7 | 튜토리얼 중복 | 함수재현 21회(shader), 쿼드/큐브 생성 5벌씩 — 튜토리얼 특성상 의도적 중복이므로 결정 필요 |

## 조치 단계

1. K<1: 엔진 버그 픽스 (B1~B9) — 작고 안전
2. K<2: 그래픽스 누수 픽스 (B12~B17)
3. K<3: 오디오 픽스 (B18)
4. K<4: 구조화 — Renderer 공용 베이스, GraphicDevice 헬퍼, SgfMath 정비
5. K<5: sgfr 튜토리얼 픽스 (B3/B4 + 기타 반환무시/리소스 누수)
6. 전체 빌드 검증 (sgf → sgfr)

## 확인 필요 (변경 전)

- 파일 5개 초과 수정 → AGENTS.md 규칙에 따라 사용자 확인
- S1(베이스 클래스 추출)과 S7(튜토리얼 중복 제거)은 큰 리팩토링이라 범위 결정 질문 필요
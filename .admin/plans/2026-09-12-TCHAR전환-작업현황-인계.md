# TCHAR 전환 작업현황 및 인계 (2026-09-12)

> 원 플랜: `.admin/plans/2026-09-11-문자열-TCHAR전환-String-StringUtil-StaticString.md`
> 기준 문서: `D:\Programming\참고 문서\01_확정_결정사항.md`
> 이 문서는 세션 재개용 실측 기록이다. 계획이 아니라 "어디까지 됐고 다음에 뭘 할지"를 적는다.

## 0. 다른 컴퓨터 시작 정보 (2026-09-12 21:30 스냅샷 — 이 문서+완료기록만으로 이어할 것)

- 시작: `git clone --recursive https://github.com/yjd6808/_YJD_Harmony.git` (GoogleBenchmark 서브모듈 필수. 없으면 gbench 컴파일 불가) 후 브랜치 `sg-main`, `git pull` (기준: `acb03579`, 원격 동기화됨)
- 첫 빌드는 전체 리빌드라 오래 걸림. `Output/`·`Intermediate/`는 git 추적 안 됨(재생성됨). gtest 실행 시 생기는 `test_io/`, `true/`는 잔재물이니 무시·커밋 금지
- 목표: §4-8의 5항 (전환 본작업은 전부 완료·푸시됨. 남은 건 검증·판단·정리뿐)
- 빌드 명령 (x64 고정, 문자셋 전환 시 `-Rebuild` 권장):
  - `Scripts\BuildProject\{프로젝트명}.bat -P x64` (Unicode)
  - `Scripts\BuildProject-Multibyte\{프로젝트명}.bat -P x64` (MultiByte)
  - MB 검증 후 Unicode로 다시 빌드 (Output/Intermediate 공유라 MB가 Unicode 산출물을 덮어씀)
  - 빌드 병렬 실행 금지 (jc.pdb C1041 충돌 전례). 순차 실행만
- 합격 기준: sgs·jdb 계열은 단독 테스트 없음, 양쪽 빌드 `[OK]`. gtest는 §5-5의 4-gate
- 세션 준수: 한국어 답변, 파일 UTF-8 저장, 5개 이상 파일 수정 전 사용자 확인, 주석에 아키텍처 결정 내용 작성 금지, `reset/clean` 금지, 커밋은 지시 시에만

## 1. 개발 규칙 (작업 중 상시 준수)

### 1-1. 제1규칙 — 문자열/TCHAR (모든 변환 작업의 최우선 기준)

1. jc 포함 전 프로젝트의 문자열 처리는 프로젝트 설정(Unicode/MultiByte)을 따라가는 `jc::String` / `const jc::_char*` / `_T()` 리터럴 기반이다. 양쪽 설정에서 분기 없이 컴파일·동작해야 한다.
2. narrow 고정은 데이터/API 자체가 narrow인 경우에만 허용한다: CRT 모드 문자열, MBCS 경로 narrow 오버로드, `_Extern` C-ABI, 파일 내용(바이트), 와이어/프로토콜 narrow 바이트, narrow 전용 외부 API 인자. 이때도 `StringUtilA` 등 명시 타입 + 경계 변환(`StringConvert`)을 쓴다.
3. WinAPI는 제네릭으로 호출한다 (`FormatMessage`/`FindFirstFile` 등, A/W 접미 직접 호출 금지). W 전용 API(WinHTTP 등)는 `String` 직접 전달이 원칙이며, MultiByte에서 깨지는 줄은 해당 줄에 한해 인라인 분기한다 (변환 헬퍼 신설 금지).
4. 인코딩 경계는 한 곳에서만 넘는다. narrow 리터럴=UTF-8, OS narrow=ANSI 로 보고, UTF-8↔`String`은 `FromUtf8`/`ToUtf8`, ANSI↔`String`은 `FromAnsi`/`ToAnsi`를 쓴다.
5. 새 `const char*` 파라미터/멤버를 만들지 않는다 (2항의 narrow 경계 제외).

### 1-2. 빌드 검증 규칙

- 모든 빌드 작업은 아래 2경로 스크립트로 Unicode/MultiByte **양쪽 성공**을 검증한다.
  - `Scripts\BuildProject\{프로젝트명}.bat` (Unicode)
  - `Scripts\BuildProject-Multibyte\{프로젝트명}.bat` (내부 `Build.ps1 -Multibyte` → `/p:CharacterSet=MultiByte`)
- 문자셋 전환 시 `-Rebuild` 권장 (Intermediate 캐시 혼용 방지).
- **주의**: `Output\x64\Debug\`와 `Intermediate\`가 문자셋 공유라 MB 빌드가 Unicode 산출물을 덮어쓴다. MB 검증 후에는 Unicode로 다시 빌드해서 트리를 기본 상태로 복구한다.

### 1-3. 세션 규칙 R9 (이번 전환 작업 세션 한정 — 원 플랜 §6-1에 R9로 기록)

- 외부 thirdparty가 narrow/wide 한쪽만 지원해서 변경 불가 지점이 나오면 우회·변환 꼼수를 넣지 말고 작업 중지 후 보고 (대상 라이브러리, 막힌 지점, 영향 범위 포함).

## 2. 사용자 결정 확정 내역

| 번호 | 내용 |
|---|---|
| Q-1 | SSO 24B 유지 (`_char` 23 배열/22문자). 48B 확대안 폐기 |
| Q-2 C안 | TCHAR 유지. cocos 표시용 테이블은 TCHAR, sgcl 변환은 엔진 교체 시점으로 이관 |
| Q-3 | `sgcl` 373파일 전부 제외. 뒤집기 이후 sgcl 빌드 실패는 예정 상태로 두고 복구하지 않음. `sgf/sgfr` 207파일, `mcp/mcpr` 35파일도 범위 밖 |
| Q-5 | Winsock 단일 TCHAR API: `ErrorMessage`/`LastErrorMessage` → `jc::String` 반환, `ErrorMessageMBCS` 삭제, 접미 없는 `FormatMessage` 매크로 |
| 추가-1 | `StringFwd.h` 신설 안 함. `String.h` 직접 include |
| 추가-2 | `Type.h`에 `UNICODE`/`_UNICODE` 동기화 가드 |
| 추가-3 | `jc_gtest`는 A/W 양쪽. gtest 1.8.1이라 `TYPED_TEST_CASE` + `typedef ::testing::Types<char,wchar_t>` 필수 |
| 추가-4 | WinHTTP는 W-only라 `String::Source()` 직접 전달이 정답 (변환 헬퍼 삭제됨) |

## 3. 완료 내역

> 완료 항목은 `.admin/plans/2026-09-12-TCHAR전환-완료기록.md`에 번호순으로 기록한다.
> 작업이 끝날 때마다 인계 문서(§4 남은 작업 갱신)와 완료기록(번호 이어서 추가)을 함께 갱신한다.
> 현재 검증 상태: 13종 + sgf + sgfr + jnetr 4종 + mcp + mcpr + jdbr Unicode/MultiByte 양쪽 빌드 `[OK]` (gbench 2종은 양쪽 ClCompile clean, 링크용 benchmark.lib 미설치), **jc 415/415 + jnet 4/4 PASSED** (2026-09-12 21:17 기준, Unicode로 재빌드됨). §4-4·§4-5·§4-6·§4-7·§4-9 완료. jcr 18건 파손으로 폐기

## 4. 남은 작업 (우선순위순)

### 4-1. `Console.h` TCHAR 전환 (완료 — 2026-09-12 11:10, 완료기록 21~22번)

- 수행: `Console.h`/`Console.cpp` 제네릭 CRT 전환 + `ThreadPool.cpp` `ToUtf8` 래핑 제거 + 호출부 정리 15파일
  (`Utils/Console.h`, `Utils/Console.cpp`, `Threading/ThreadPool.h`, `Threading/ThreadPool.cpp`,
  `Utils/ConsoleMenuItem.cpp`, `Threading/Coroutine.h`, `Threading/Coroutine.cpp`,
  `Pool/MemoryPoolAbstract.h`, `Wrapper/WinApi.cpp`, `_Extern/Extern_Console.cpp`,
  `Logger/ConsoleLogger.cpp`, `Threading/ThreadLocal.h`,
  `jc_gtest/Threading/ThreadPoolTest.cpp`, `jc_gtest/Threading/SchedulerTest.cpp`,
  `jc_gtest.vcxproj` obj 분리)
- 검증: jc + jc_gtest 양쪽 빌드 `[OK]`, 전체 실행 양쪽 EXIT 0, **415/415 PASSED**, Unicode 재빌드 복구됨
- 다음은 §4-2 순서대로 (`Assert` D-1부터)

### 4-2. 원 플랜 Phase 8 잔여 (별도 plan 분리 예정 — 순서 고정)

1. `Assert` 부분 전환 (D-1) — 완료 (2026-09-12 11:20, 완료기록 23번, C안 sgcl 제외). `_fmt`만 `_char*`, narrow 유지 + `%hs` 출력, 호출부 441곳 일괄 `_T()`화
2. 파일 I/O 경로 TCHAR화 (D-4) — 완료 (2026-09-12 11:25, 완료기록 24번). `File::Move`/`Copy` String 본문 `_char` 직결, narrow API는 경계로 유지
3. jc 전역 `_T()` + `%hs` 전환 잔여분 (R2) — 완료 (2026-09-12 11:30, 완료기록 25번). 잔재 2파일만 전환, 나머지 narrow 확정

### 4-3. 범위 밖 / 별도 판단 필요

- `Path.h`의 `const char*` 오버로드 잔재 — **유지로 결정** (2026-09-12 11:40). narrow 리터럴 직접 호출 0건(전수 grep), 유일 사용처 `Assert.cpp:33` buf판은 no-alloc assert 경로라 유지가 정답. String판 일원화(삭제) 시 이득 없음
- `jnet` — 완료 (2026-09-12 11:55, 완료기록 27번. R8 + Q-5 포함)
- `jdb` — 완료 (2026-09-12 12:15, 완료기록 28번. MySQL Unicode 제외, `JDB_HAS_MSODBCSQL` 가드. `jdbr`은 기존 C2131로 제외)
- `StringUtil` 개명 + `Comparator.inl` + `StringView` 뒤집기 — 완료 (2026-09-12 13:00경, 완료기록 29번)
- `sg`/`sge` — 완료 (2026-09-12 14:00, 완료기록 30번. 계획 `.admin/plans/2026-09-12-sg-TCHAR전환.md`, 실측 576건/39파일)
- `sgs` 본체 — 완료 (2026-09-12 14:10, 완료기록 31번. sgs_auth/center/lobby/game/query sed 일괄 적용)
- `sgs_auth` — 완료 (2026-09-12 15:26, 완료기록 32번, §4-4)

### 4-4. `sgs_auth` Unicode/MultiByte 양쪽 빌드 `[OK]` (완료 — 2026-09-12 15:26, 완료기록 32번)

수정 1 — 계정 ID 체인 `_char` 일괄 (인계안 그대로, `R_AUTHENTICATION.cpp`·`R_LOBBY.cpp` 호출부는 서명 변경만으로 해소되어 무수정):

1. `sgs_auth/AuthenticationManager.h` + `.cpp` — `FindRaw`/`UpdateRaw`/`Exist`/`Update`/`Remove`의 `const char* _pAccountId` 6곳 → `const _char*`. `.cpp(195)` C2678 `==`, `(213)` C2678 `!=`는 wide끼리 비교로 해소. `(304)` C2446 포인터 비교는 컴파일만 되고 의미는 손대지 않음(기존 버그, 범위 밖)
2. `sgs/AuthenticationComponent.h` + `.cpp` — `SetAccountId(const _char*)`, 본문 `FromUtf8` 브릿지 제거 후 직접 전달, `StringConvert.h` include 제거
3. `sgs_lobby/S_AUTHENTICATION_IS.h` + `.cpp` — `SEND_SAU_AuthenticationCheck(..., const _char*, ...)`
4. `sgs_auth/Q_LOGIN.h` + `.cpp` — 선언 4종 `const _char*`, 본문 MySQL 참조부를 `#ifndef _UNICODE`로 감싸고 `#else`에 assert + 실패 반환 (jdb 패턴)
5. `sg/LogSpecifier.h` — `LOG_SPECIFIER_GAME` `%hs` 유지 확정, 무수정 (`GameServerType::Name` narrow + sgcl 제외 범위라 `_char`화는 범위 밖)

수정 2 — jdb B안 (사용자 승인: A-API 고정 해제, jdb 전체 TCHAR. 계획 `.admin/plans/2026-09-12-jdb-TCHAR전환.md`):

- L1: `IDatabase.h` shim → `const _char*` + `stmt.info_.text_ = stmt.text_`, `sgs/QueryDefine_AUTH.h` `Script` 3곳 + 컬럼 리터럴 10곳 `_T()`화
- L2: `QueryDefine.h` `BindField` → `GetString` 경유, `StatementType.h` `StatementName` `_char` + `%hs`→`%s`
- L3: `IQuery.h` 필드명 4종 + `SqlServerQuery.h/.cpp` 오버라이드 → `const _char*` (`GetRawString` 반환·읽기 경로 `SQL_C_CHAR`·`FromAnsi`는 narrow 유지, `colNameMap_` 공집합 동작 동일)
- L4: `SqlServerStatementBuilder.h` `Setter` narrow 3종 → `const _char*` + 직접 전달
- L5: ODBC 제네릭 (`SQLExecDirect`/`SQLDriverConnect`/`SQLGetDiagRec`/`SQLDescribeCol` + `SQLTCHAR`, 문자수는 `sizeof/sizeof(SQLTCHAR)`), `ToAnsi` 9곳 제거 후 `String` 직접 전달. MySQL 파일(`#ifndef _UNICODE` 제외)은 미변경
- MB 1차 실패 `IDatabase.h:73` C2679 → B안으로 해소. MB 2차 실패 `SQLCHAR(unsigned char)` C2440 4곳 → `(_char*)` 캐스트로 해소
- 검증: jdb + sgs_auth Unicode/MultiByte 양쪽 빌드 `[OK]`, Unicode 재빌드 복구됨
- 다음 §4-5에서 sgs_center/lobby/game/query 각자 `*.bat` 양쪽 빌드로 오류 소진 (동일 패턴)

### 4-5. `sgs_center`/`sgs_lobby`/`sgs_game`/`sgs_query` 양쪽 빌드 `[OK]` (완료 — 2026-09-12 15:53, 완료기록 33번)

- `sgs_center`·`sgs_lobby`·`sgs_query` — 수정 없이 양쪽 `[OK]` (sed 일괄분이 그대로 통과)
- `sgs_game` — `main.cpp` 1파일 2곳 수정 후 양쪽 `[OK]`. `parse_args(67)` C2672: CRT narrow 인자(`_pArgv[1]`)라 `StringUtil::TryToNumber` → `StringUtilA::TryToNumber` (명시 narrow, ASCII 숫자라 변환 불필요). `(70)` narrow 인자 `%s` → `%hs` (Unicode 실행 깨짐 방지)
- 4종 Unicode 재빌드 복구됨. 서버 5종(auth/center/lobby/game/query) TCHAR 전환 완료
- 다음 §4-6 잔무 (TCHAR 무관 정리는 별도 커밋으로 분리)

### 4-6. 잔무 정리 (완료 — 2026-09-12 16:03, 완료기록 34번)

- `jc/Type.h` 중복 using 3행 제거 (`_s32`·`_s8`·`_u8` 2회 선언 → 1회. 동일 별칭이라 무의미 변경)
- `sg/Sources/sg/Config.h` + `Config.cpp` 삭제 (구 `ConfigRuntime`/`ConfigArgs`. `Core/AppConfig.*`와 중복, vcxproj 미등록·외부 참조 0건 전수 확인. sgcl이 쓰는 건 `g_cAppConfig`라 영향 없음)
- `sgs_center/R_SETUP.cpp` narrow `%s` 4곳 → `%hs` (`ServerType`/`ServerBootState`/`CenterOrder::Name` 3종 narrow 확인. game main.cpp 동일 패턴)
- 검증: sg + sgs_center Unicode/MultiByte 양쪽 빌드 `[OK]` (Type.h 전역 헤더라 sg 체인, Config 삭제라 sg, R_SETUP이라 center), Unicode 재빌드 복구됨

### 4-7. 전체 13종 최종 검증 sweep (완료 — 2026-09-12 17:01, 완료기록 35번)

- `jc`·`jc_gtest`·`jnet`·`jnet_gtest`·`jdb`·`sg`·`sge`·`sgs`·`sgs_auth`·`sgs_center`·`sgs_lobby`·`sgs_game`·`sgs_query` Unicode → MultiByte → Unicode 3-pass, 39/39 `[OK]`, 수정 0건
- gtest 전체 실행은 제외. 14:00 이후 jc/jnet 동작 변경이 없어서(§4-6 `Type.h`는 무의미 변경, jdb B안·서버 수정은 gtest 범위 밖) 415/415 + 4/4 기록 유지
- Unicode 재빌드 복구됨. TCHAR 전환 범위 내 전 프로젝트 양쪽 빌드 확정

### 4-8. 다음 작업 (2026-09-12 21:30 확정 — 다른 컴퓨터에서 이 순서대로)

1. gtest 전체 실행 (미실행): `jc_gtest` 415 + `jnet_gtest` 4, 양쪽 문자셋, §5-5 절차+4-gate. exe `Output\x64\Debug\` (없으면 해당 bat로 빌드). MB 실행 후 Unicode 재빌드 복구. 통과하면 완료기록 38번 + §3 갱신
2. `%hs` 한글 깨짐 판단 (실측됨, 미결정): C 로케일 `%hs`는 바이트 identity라 한글 narrow 깨짐. 대상 `sg/LogSpecifier.h`(`LOG_SPECIFIER_GAME`) + `sgs_center/R_SETUP.cpp` 4곳 + narrow 원본(`sg/Struct/SteinsGate_Server.h` `ServerType`/`GameServerType::Name`, `sgs/ServerEnum.h` `CenterOrder`/`ServerBootState::Name`). 선택지: (a) 유지 (b) `_char` 테이블화 — sgcl 공용 헤더라 Q-3 논의 필요 (c) 사용처 `FromUtf8` (narrow 리터럴은 `/utf-8` 플래그로 UTF-8 확정됨). 결정 후 작업·검증·기록
3. ODBC W 한글 왕복 실검증 (live DB 필요): `sgs_query` 실쿼리 작성 시점. 빌드만 통과 상태
4. dead 정리 후보 (동작 변경이라 별도 승인 필요, 손대지 않음): `Q_LOGIN` nullptr 스텁(`sgs_auth/Q_LOGIN.cpp`), `colNameMap_` 공집합(`jdb/SQLServer/SqlServerQuery.cpp:166`), `AuthenticationManager.cpp:304` 포인터 비교
5. gbench 링크 (환경 사유 보류): `benchmark.lib` 미설치. `Scripts/BuildBenchmark`는 ThirdParty 전체 리빌드(rmdir 포함)로 무거우니 지시 없으면 실행 금지. ClCompile 양쪽 clean으로 대체 검증됨
6. 범위 밖 유지 (손대지 않음): `sgcl` 빌드 실패 예정 (Q-3), jdbr 제외 해제됨(§4-9 양쪽 통과)

### 4-9. 범위밖 TCHAR 전환 (완료 — 2026-09-12 21:17, 완료기록 36번, 계획 `.admin/plans/2026-09-12-범위밖-TCHAR전환.md`)

- sgf 24파일: 헤더 기본인자·본문 `_T()` 누락 소진. Unicode/MultiByte `[OK]`
- sgfr 45파일: 한글 테이블 `_char`+`_T()`, Format/WriteLine/Create/SetTitle/_Log `_T()`, HLSL `FromUtf8`, narrow CRT 유지, `%hs`는 ASCII만. Main.cpp stray `}` 1개 제거(타 세션 편집 잔재). Unicode/MultiByte `[OK]`
- jnetr 12파일: 수정 없이 양쪽 `[OK]` (dual-charset 정상 확인 — 오판 정정 기록)
- mcp 1파일 + mcpr: 양쪽 `[OK]`
- gbench 15+8파일: `StrForBench` narrow 확정(`StringUtilA`), `BenchR0=jc::AString`(바이트 데이터셋 정합). 양쪽 ClCompile clean. 링크는 benchmark.lib 미설치로 불가(환경 사유, `Scripts/BuildBenchmark` 미실행)
- jdbr: 양쪽 `[OK]`. 기존 C2131 해소됨
- jcr 18건: sed 파손(`_T("$3")` 30곳) 확인 후 폐기(checkout, 커밋 제외)
- 부수 수정: `jc/Utils/Console.h:244` `Math::Min<int>` (Unicode 배열-WriteLine 전용 잠재 버그. MB는 int,int라 은폐됨)
- 검증: 각 영역 Unicode → MultiByte → Unicode 재빌드 복구됨. sgcl은 제외 유지

## 5. 함정 / 노하우 (재작업 방지)

1. `StaticString::CopyFrom` 1인자 배열 오버로드는 삭제된 상태 — 배열 인자 함정 때문에. 다시 만들지 말 것
2. gtest 1.8.1: `TYPED_TEST`는 `TYPED_TEST_CASE` + 파일 상단 `typedef ::testing::Types<char,wchar_t>` 필요
3. 새 `.cpp`를 프로젝트에 추가하면 `.vcxproj` `ClCompile` 등록 확인 (`StringConvert.cpp` 누락 전례)
4. jc `assert` (`jc_assert_msg`) 발동 시 프로세스가 EXIT 3으로 종료되고 gtest 요약이 안 찍힘 — 멈춘 지점의 바로 앞 `[ RUN ]`이 범인
5. gtest 로그는 UTF-16으로 떨어짐. 분석 시 `utf-16`으로 읽어 `utf-8`로 변환 후 grep (`$env:TEMP\gtest_*.txt` 활용 전례)
   - 검증 절차 (아래 §6-2 with 동일): exe 출력을 파일로 리다이렉트 → UTF-16→UTF-8 변환 → 집계.
     합격 기준 4개 모두 충족: ① 프로세스 EXIT 0 ② `RUN` 수 == `OK` 수 (기준 시점 415) ③ `FAILED` 0건 ④ `[ PASSED ] 415 tests.` 존재.
     `RUN`은 있는데 `OK`/`FAILED`가 없으면 해당 테스트에서 assert·크래시로 중단된 것 → 멈춘 지점 바로 앞 `[ RUN ]`부터 원인 추적 (함정 4번).
     `OK`만 세지 말고 `RUN` 수도 함께 셀 것 (중단 시 `OK`만 보면 통과처럼 보임)
6. CRT leak 덤프 (`Dumping objects`, `normal block`)는 테스트 실패가 아님. `FAILED` 카운트로 판단
7. `ThreadLocal<int>` 해쉬맵 초기화 로그 등 한글 로그가 섞여 있어 파싱 시 인코딩 주의
8. `Console.h`처럼 헤더에 `printf`/`putchar`가 남아 있으면 MultiByte 빌드는 통과해도 Unicode에서 깨짐 — 양쪽 "실행"까지는 아니더라도 양쪽 "빌드"는 필수
9. `StaticString<N,_char>` ↔ `const char*`는 `==`/`!=` 연산자가 없어 C2678 (sgs_auth 전례). 계정 ID처럼 ASCII narrow가 흘러다니는 체인은 파라미터를 `const _char*`로 일괄 전환 (호출자가 패킷 wide 배열 1곳이면 본문 직접 전달이 최단)
10. MySQL 제외 파일(jdb 패턴 `#ifndef _UNICODE`)을 쓰는 상위 호출자(sgs_auth `Q_LOGIN`)는 헤더 선언 + 호출부 인자까지 함께 손봐야 함. 선언만 narrow로 두면 C2664가 호출부로 옮겨감
11. `SQLCHAR`는 `unsigned char`라 `String` 직접 대입이 C2440. `jc::String((_char*)버퍼)`로 보정 (MB narrow·Unicode wide 양쪽 통과, SqlServer 전례)
12. ODBC 버퍼 길이는 문자 수 기준. `sizeof(버퍼)` 대신 `sizeof(버퍼)/sizeof(SQLTCHAR)` (MB 동일·Unicode 오버런 방지)
13. 파일 삭제 전 3종 확인: ① 전 vcxproj 미등록 ② `#include`·심볼 무참조 전수 grep (동명이인 클래스 주의 — `Config` vs `AppConfig` 전례) ③ 삭제 후 `git status` D 확인. 삭제 파일은 인계 §0에 의도된 삭제라고 명시 (다음 세션 복구 방지)

## 6. 작업 재개 절차 (다른 컴퓨터용)

1. §0대로 clone·pull 후 §4-8 1번부터 시작 (전환 본작업 완료. 남은 건 검증·판단·정리)
2. 빌드가 필요하면 Unicode → MultiByte → Unicode 재빌드 (병렬 실행 금지)
3. gtest 전체 실행은 §5-5 절차 + 4-gate (① EXIT 0 ② RUN==OK ③ FAILED 0 ④ PASSED 수 일치)
4. 완료되면 이 문서 §3/§4 갱신 + 완료기록 번호 이어서 추가 (다음 번호: 38번)
